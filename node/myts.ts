
let x:number = 10

function sum(x:number, y: number):number {
    return x + y
}

let a:string[] = ['abc']

interface ICars {
    wheels?: number,
    color: string | number
}

let car:ICars = {
    wheels: 10,
    color: 'red'

}